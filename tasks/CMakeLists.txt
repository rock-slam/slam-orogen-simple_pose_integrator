# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(simple_pose_integratorTaskLib)
ADD_LIBRARY(${SIMPLE_POSE_INTEGRATOR_TASKLIB_NAME} SHARED
    ${SIMPLE_POSE_INTEGRATOR_TASKLIB_SOURCES})
add_dependencies(${SIMPLE_POSE_INTEGRATOR_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${SIMPLE_POSE_INTEGRATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SIMPLE_POSE_INTEGRATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SIMPLE_POSE_INTEGRATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SIMPLE_POSE_INTEGRATOR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SIMPLE_POSE_INTEGRATOR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SIMPLE_POSE_INTEGRATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SIMPLE_POSE_INTEGRATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SIMPLE_POSE_INTEGRATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/simple_pose_integrator)

