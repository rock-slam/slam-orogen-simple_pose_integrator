# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(constant_pose_providerTaskLib)
ADD_LIBRARY(${CONSTANT_POSE_PROVIDER_TASKLIB_NAME} SHARED 
    ${CONSTANT_POSE_PROVIDER_TASKLIB_SOURCES})
add_dependencies(${CONSTANT_POSE_PROVIDER_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${CONSTANT_POSE_PROVIDER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${CONSTANT_POSE_PROVIDER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CONSTANT_POSE_PROVIDER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CONSTANT_POSE_PROVIDER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${CONSTANT_POSE_PROVIDER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${CONSTANT_POSE_PROVIDER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CONSTANT_POSE_PROVIDER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CONSTANT_POSE_PROVIDER_TASKLIB_HEADERS}
    DESTINATION include/orocos/constant_pose_provider)

